{% extends 'base.html.twig' %}

{% block body %}
    <div class="outer">
        <div class="middle">
            <div class="inner">
                <img alt="AXETA" src="{{ asset('svg/axeta.svg') }}">
            </div>
        </div>
    </div>
{% endblock %}
